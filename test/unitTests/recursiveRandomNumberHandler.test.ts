//import {recursiveRandomNumberHandler} from "../../src/service/recursiveRandomNumberHandler";
//import express from "express";
//import {app} from "../../src/app";
/*jest.mock('../../src/app');
jest.mock('../../src/configurationConstants');

jest.mock('../../src/app', () => {
    return {
        get: jest.fn(() => 42),
        set: jest.fn(() => 43),
        router: jest.fn(() => 43),
    };
});

describe('recursiveRandomNumberHandler', () => {
    it('when', () => {
        recursiveRandomNumberHandler()
    });

});*/
